{
  "name": "telecode",
  "displayName": "Telecode",
  "description": "AI Coding Continuity: Mobile to Desktop Bridge",
  "version": "0.0.1",
  "publisher": "telecode",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "telecode.connect",
        "title": "Telecode: Connect with Sync Code"
      },
      {
        "command": "telecode.disconnect",
        "title": "Telecode: Disconnect"
      },
      {
        "command": "telecode.refreshTasks",
        "title": "Telecode: Refresh Tasks",
        "icon": "$(refresh)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "telecode-explorer",
          "title": "Telecode",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "telecode-explorer": [
        {
          "id": "telecode-tasks",
          "name": "Active Tasks"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "telecode.refreshTasks",
          "when": "view == telecode-tasks",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Telecode",
      "properties": {
        "telecode.apiUrl": {
          "type": "string",
          "default": "http://localhost:3000",
          "description": "Telecode Server API URL"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "echo \"No tests for extension\""
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "eslint": "^8.28.0",
    "glob": "^8.0.3",
    "mocha": "^10.1.0",
    "typescript": "^4.9.3",
    "@vscode/test-electron": "^2.2.0",
    "axios": "^1.2.1"
  },
  "dependencies": {
    "axios": "^1.2.1"
  }
}
